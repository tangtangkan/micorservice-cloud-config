# 在记事本上编写不要用tab键进行缩进
spring:
  profiles:
    active: dev # 激活开发环境配置

server:
  port: 4001 #端口号
spring:
  profiles: dev # 开发环境
  application:
    name: microservice-config-dev

server:
  port: 4002 #端口号
spring:
  profiles: prod # 开发环境
  application:
    name: microservice-config-prod